# Настройка Telegram для контактной формы

## Быстрый старт

### 1. Создай бота

1. Открой Telegram → найди **@BotFather**
2. Отправь `/newbot`
3. Введи имя: `Si Mosaic Forms`
4. Введи username: `simosaic_forms_bot`
5. **Скопируй Token**: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2. Получи Chat ID

**Личные сообщения:**
```
1. Найди бота → отправь /start
2. Открой: https://api.telegram.org/bot<TOKEN>/getUpdates
3. Найди: "chat":{"id":123456789}
```

**Групповой чат:**
```
1. Создай группу → добавь бота (сделай админом)
2. Отправь сообщение в группу
3. Открой: https://api.telegram.org/bot<TOKEN>/getUpdates
4. Найди: "chat":{"id":-100123456789}
```

### 3. Добавь в wp-config.php

В `wp-config.php` **перед** `/* That's all, stop editing! */`:

```php
// Telegram Bot
define('MOSAIC_TELEGRAM_BOT_TOKEN', '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz');
define('MOSAIC_TELEGRAM_CHAT_ID', '123456789'); // или -100123456789 для группы
```

### 4. Готово!

Теперь при отправке формы:
- ✅ Заявка сохраняется в БД
- ✅ Уведомление приходит в Telegram
- ✅ В админке доступен список всех заявок (меню "Заявки")

## Проверка работы

1. Зайди на сайт
2. Заполни форму "Давайте обсудим ваш проект"
3. Отправь
4. Проверь:
   - Telegram (должно прийти уведомление)
   - Админка → Заявки (должна появиться запись)

## Возможные проблемы

### "Заявка не отправлена"

Проверь логи WordPress:
```php
// В wp-config.php включи debug:
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```

Логи будут в `wp-content/debug.log`

### "Telegram не получает сообщения"

1. Проверь токен и Chat ID в `wp-config.php`
2. Убедись, что бот **запущен** (отправь ему `/start`)
3. Для групп — бот должен быть **администратором**
4. Проверь `/getUpdates` — есть ли в ответе твой Chat ID

### "Заявки не сохраняются в БД"

Таблица создаётся автоматически при активации темы.
Если не создалась:
1. Деактивируй тему
2. Активируй заново
3. Проверь в БД наличие таблицы `wp_mosaic_contacts`

## Админка заявок

**Функции:**
- 📋 Список всех заявок с датой/временем
- 📧 Клик по email/телефону → открывает почту/звонок
- 📥 Экспорт в CSV
- 🗑️ Удаление заявки

**Доступ:** WordPress админка → **Заявки** (в боковом меню)

## Безопасность

- ✅ Nonce проверка
- ✅ Санитизация всех полей
- ✅ Prepared statements для БД
- ✅ IP и User-Agent логируются
- ✅ Bot token хранится в wp-config.php (не в репозитории!)

**Важно:** Никогда не коммить `wp-config.php` в Git!

